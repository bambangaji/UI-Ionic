import { GetCodeCountryPipe } from './get-code-country.pipe';

describe('GetCodeCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCodeCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
