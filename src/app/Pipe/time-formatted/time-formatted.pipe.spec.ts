import { TimeFormattedPipe } from './time-formatted.pipe';

describe('TimeFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
