import { ArraySearchPipe } from './array-search.pipe';

describe('ArraySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
